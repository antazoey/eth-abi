[build-system]
requires = ["setuptools>=75", "wheel", "setuptools_scm[toml]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "eth_abi"
dynamic = ["version"]
description = "eth_abi: Python utilities for working with Ethereum ABI definitions, especially encoding and decoding"
readme = "README.md"
requires-python = ">=3.10,<4"
license = "MIT"
authors = [
    { name = "The Ethereum Foundation", email = "snakecharmers@ethereum.org" },
    { name = "ApeWorX Collective", email = "admin@apeworx.io" },
]
maintainers = [
    { name = "ApeWorX Collective", email = "admin@apeworx.io" },
]
keywords = ["ethereum"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Natural Language :: English",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "eth-utils>=2.0.0",
    "eth-typing>=3.0.0",
    "parsimonious>=0.10.0,<0.11.0",
]

[project.urls]
Homepage = "https://github.com/ApeWorX/eth-abi"
Repository = "https://github.com/ApeWorX/eth-abi.git"
Issues = "https://github.com/ApeWorX/eth-abi/issues"
Documentation = "https://eth-abi.readthedocs.io/"
Funding = "https://github.com/sponsors/ApeWorX"

[project.optional-dependencies]
tools = [
    "hypothesis>=6.22.0,<6.108.7",
]

[dependency-groups]
test = [
    "pytest>=7.0.0",
    "pytest-timeout>=2.0.0",
    "pytest-xdist>=2.4.0",
    "pytest-pythonpath>=0.7.1",
    "eth-hash[pycryptodome]",
    "hypothesis>=6.22.0,<6.108.7",
]
lint = [
    "ruff>=0.12.0",
    "mypy==1.18.2",
    "mdformat>=0.7.22",
    "mdformat-gfm>=0.3.5",
    "mdformat-frontmatter>=0.4.1",
    "mdformat-pyproject>=0.0.2",
]
docs = [
    "sphinx>=6.0.0",
    "sphinx-autobuild>=2021.3.14",
    "sphinx_rtd_theme>=1.0.0",
]
dev = [
    { include-group = "test" },
    { include-group = "lint" },
    { include-group = "docs" },
    "ipython",
    "prek",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
exclude = ["scripts", "scripts.*", "tests", "tests.*"]

[tool.setuptools.package-data]
eth_abi = ["py.typed"]

[tool.setuptools_scm]
fallback_version = "6.0.0b1"

[tool.mypy]
check_untyped_defs = true
disallow_any_generics = true
disallow_incomplete_defs = true
disallow_subclassing_any = true
disallow_untyped_calls = false
disallow_untyped_decorators = true
disallow_untyped_defs = false
ignore_missing_imports = true
strict_equality = true
strict_optional = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = true
disable_error_code = [
    "arg-type",
    "call-arg",
    "call-overload",
    "assignment",
    "operator",
    "var-annotated",
    "attr-defined",
    "has-type",
    "union-attr",
    "type-arg",
]

[tool.pytest.ini_options]
addopts = "-v --showlocals --durations 10"
log_date_format = "%m-%d %H:%M:%S"
log_format = "%(levelname)8s  %(asctime)s  %(filename)20s  %(message)s"
xfail_strict = true

[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [".project-template", "docs/conf.py"]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "W",
    "B",
    "I",
    "UP",
]

[tool.ruff.lint.isort]
known-first-party = ["eth_abi"]
known-third-party = ["hypothesis"]

[tool.ruff.lint.per-file-ignores]
"eth_abi/__init__.py" = ["F401"]
"eth_abi/tools/__init__.py" = ["F401"]

[tool.ruff.format]
quote-style = "double"
line-ending = "auto"
indent-style = "space"
docstring-code-format = true

[tool.mdformat]
number = true
